{
  "type": "module",
  "local": {
    "fastify-vite": "^3.0.0-beta.8"
  },
  "scripts": {
    "dev": "node server.js dev",
    "build:client": "vite build --outDir dist/client --ssrManifest",
    "build:server": "vite build --ssr /entry/server.js --outDir dist/server"
  }
}
