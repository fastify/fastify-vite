{
  "name": "examples",
  "private": true,
  "scripts": {
    "dev": "node server",
    "dev:install": "cp ../../*.js node_modules/fastify-vite && cp -r ../../renderers node_modules/fastify-vite && cp -r ../../vue node_modules/fastify-vite && cp -r ../../react node_modules/fastify-vite",
    "build": "node server build",
    "serve": "cross-env NODE_ENV=production node server",
    "debug": "node --inspect-brk server",
    "test": "tap test.js"
  },
  "dependencies": {
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-helmet": "^6.1.0",
    "react-router-dom": "^5.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react-refresh": "^1.3.1",
    "cross-env": "^7.0.3",
    "fastify": "^3.13.0",
    "fastify-api": "^0.1.0",
    "fastify-vite": "^2.0.3",
    "vite": "^2.0.5"
  }
}
