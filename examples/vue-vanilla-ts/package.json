{
  "name": "example-vue-vanilla-ts",
  "type": "module",
  "scripts": {
    "dev": "tsx src/server.ts --dev",
    "start": "node dist/server.js",
    "build": "pnpm build:client && pnpm build:server",
    "build:server": "vue-tsc && tsdown",
    "build:client": "vite build --emptyOutDir",
    "lint": "oxlint",
    "test": "tsx --test"
  },
  "dependencies": {
    "@fastify/vite": "workspace:^",
    "fastify": "catalog:",
    "vue": "catalog:"
  },
  "devDependencies": {
    "@types/node": "^22.13.17",
    "@vitejs/plugin-vue": "catalog:",
    "oxlint": "catalog:",
    "tsdown": "catalog:",
    "tsx": "catalog:",
    "vite": "catalog:",
    "vue-tsc": "^2.2.8"
  }
}