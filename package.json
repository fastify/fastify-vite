{
  "scripts": {
    "test": "vitest run examples/*/*.test.js --no-threads"
  },
  "name": "fastify-vite",
  "devDependencies": {
    "execa": "^6.1.0",
    "vitest": "^0.24.0",
    "zx": "^4.3.0"
  }
}
