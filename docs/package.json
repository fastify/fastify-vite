{
  "name": "docs",
  "scripts": {
    "docs:dev": "vitepress dev .",
    "docs:build": "vitepress build . && node covers.js && cp -r covers/*.png .vitepress/dist/"
  },
  "dependencies": {
    "autoprefixer": "^10.3.2",
    "convert-svg-to-png": "^0.5.0",
    "klaw": "^4.0.1",
    "postcss-nesting": "^8.0.1",
    "vitepress": "^0.16.1"
  }
}
