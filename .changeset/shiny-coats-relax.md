---
"@fastify/vite": major
---

Remove ssrManifest. `@fastify/htmx` will no longer work with this, but it was already broken anyway and I have no permission to publish new version of it. This can be re-visited when work can be resumed here.
