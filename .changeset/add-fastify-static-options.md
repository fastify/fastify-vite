---
"@fastify/vite": minor
---

Add `fastifyStaticOptions` for configuring `@fastify/static` in production mode. This allows passing options like `preCompressed`, `maxAge`, and `immutable` through to the underlying static file server.
